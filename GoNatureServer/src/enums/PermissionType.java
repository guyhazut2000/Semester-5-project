package enums;

/**
 * * in this class, we list the different permission types in our system so we
 * can distinguish between different commands.
 * 
 * @version 1.0
 * @author group 20
 *
 */
public enum PermissionType {
	
	AddInvite, GetParkCapacity, GenerateReport, CreateRequest, CreateEvent, SetParameter
}
